class User {
    userId;
    name;
    userSocket;
    constructor(userId, name, socket) {
        this.userId = userId;
        this.name = name;
        this.userSocket = socket;
    }
}
export default User;
//# sourceMappingURL=User.js.map