class User {
    userId;
    name;
    socket;
    constructor(userId, name, socket) {
        this.userId = userId;
        this.name = name;
        this.socket = socket;
    }
}
export default User;
//# sourceMappingURL=User.js.map