class User {
    userId;
    email;
    userSocket;
    constructor(userId, email, socket) {
        this.userId = userId;
        this.email = email;
        this.userSocket = socket;
    }
}
export default User;
//# sourceMappingURL=User.js.map